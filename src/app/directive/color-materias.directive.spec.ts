import { ColorMateriasDirective } from './color-materias.directive';

describe('ColorMateriasDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorMateriasDirective();
    expect(directive).toBeTruthy();
  });
});
